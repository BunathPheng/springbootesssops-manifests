---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootesssops                  # Ingress name
  labels:
    app: springbootesssops
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootesssops.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootesssops               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
